/** @type {import("prettier").Config} */

const config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: true,
  printWidth: 100,
  singleQuote: true,
  bracketSpacing: true,
  useTabs: false,
  endOfLine: 'auto',
  arrowParens: 'avoid',
};

export default config;
